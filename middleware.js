// Middleware disabled for Phase 2 — client-side auth protection via AuthContext.
// Will be re-enabled with cookie-based sessions in a future phase.

export function middleware() {
  // Pass through all requests
}

export const config = {
  matcher: [],
};
